pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.3' }
  }
  stages {
    stage('Rollback') {
      steps {
        sh '''
          liquibase rollback version1
        '''
      }
    }
  }
}