pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.3' }
  }
  stages {
    stage('Rollback') {
      steps {
        sh 'COPY postgresql-42.2.23.jar liquibase/liquibase:4.4.3/lib'
        sh 'liquibase rollback version1'
      }
    }
  }
}