pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.3', image 'alpine/git' }
  }
  stages {
    stage('Snaphot from Database') {
      steps {
        sh '/usr/bin/git status'
      }
    }

  }
}