pipeline {
  agent none
  stages {
    stage('Snaphot from Database') {
      steps {
        sh """
          #!/bin/bash
          echo "git status"
          /usr/bin/git status
        """
      }
    }

  }
}