pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.3' }
  }
  stages {
    stage('Snaphot from Database') {
      steps {
        sh """
          #!/bin/bash
          echo "git status"
          ls -la
          which java
          which git
        """
      }
    }

  }
}