pipeline {
  stages {
    stage('Snaphot from Database') {
      steps {
        sh """
          #!/bin/bash
          echo "git status"
          git status
        """
      }
    }

  }
}