pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.3' }
  }
  stage('Update GIT') {
  steps {
        script {
          catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
            sh """
          #!/bin/bash
          echo "git status"
          /usr/bin/git status
        """
          }
        }
      }
    }

}