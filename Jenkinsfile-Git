pipeline {
  agent {
    docker { image 'alpine/git' 
      args '-it --entrypoint=/bin/bash'
    }
  }

  stages {
    stage('Git Status') {
      steps {
        sh 'git status'
      }
    }
  }
}

