pipeline {
  agent {
    docker { image 'alpine/git' 
      args '-it --entrypoint=/bin/sh alpine git'
    }
  }

  stages {
    stage('Git Status') {
      steps {
        sh 'git status'
      }
    }
  }
}

