pipeline {
  agent {
    docker { image 'bitnami/git:latest' 
      --args '-it --entrypoint=/bin/sh'
    }
  }

  stages {
    stage('Git Status') {
      steps {
        sh 'git status'
      }
    }
  }
}

